export default defineAppConfig({
    ui: {
        colors: {
            primary: 'yellow',
            gray: 'neutral'
        },

        modal: {
            base: "items-center" // container: "items-center"
        },

        button: {
            base: "cursor-pointer"
        },
        select: {
            base: "cursor-pointer"
        },
        checkbox: {
            base: "cursor-pointer"
        }        
    }
})
