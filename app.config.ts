export default defineAppConfig({
    ui: {
        colors: {
            primary: 'yellow',
            gray: 'neutral'
        },

        modal: {
            base: "items-center" // container: "items-center"
        }
    }
})
